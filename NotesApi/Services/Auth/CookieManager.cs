namespace NotesApi.Services.Auth;

public class CookieManager
{
    // public static void SetCookie(HttpResponse response, string cookie, string paramName, object paramValue, DateTime expires)
    // {
    //     response.Cookies.Append(cookie, GetCookieName(paramName, paramValue.ToString()), new CookieOptions
    //     {
    //         Expires = expires
    //     });
    // }
    
}